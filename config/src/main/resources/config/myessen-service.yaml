api:
  security:
    token:
      secret: ${JWT_SECRET:any-secret}
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
auth0:
  audience: https://myessen.eu.auth0.com/api/v2/
  domain: myessen.eu.auth0.com
server:
  port: 8082
spring:
  datasource:
    password: pepeeselmejor
    url: jdbc:postgresql://localhost:5432/myessen
    username: postgres
management:
  tracing:
    sampling:
      probability: 1.0