eureka:
  client:
    enabled: false
spring:
  flyway:
    enabled: false
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
  datasource:
    url: jdbc:h2:mem:tests
    username: "test"
    password: "password"
    driverClassName:  org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  defer-datasource-initialization: true