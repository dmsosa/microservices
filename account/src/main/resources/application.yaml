spring:
  application:
    name: account
  config:
    import: optional:configserver:/
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/